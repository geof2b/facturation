App.Settings = (() => {
  const init = () => {
    // La logique de gestion des données a été déplacée vers le serveur.
    // Cette page peut être utilisée pour d'autres paramètres, comme les bonus.
    console.log("Page des paramètres initialisée.");
  };

  return { init };
})();
